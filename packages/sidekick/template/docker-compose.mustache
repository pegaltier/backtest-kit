services:
  app:
    image: {{PROJECT_NAME}}/{{PROJECT_NAME}}:latest
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - "./logs:/app/logs"
      - "./dump:/app/dump"
      - "./config:/app/config"
    env_file:
      - .env
    restart: always
